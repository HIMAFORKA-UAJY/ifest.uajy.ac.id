import { FC } from "react";
import Layout from "../components/Layout";

const I2C: FC = () => {
    return (
        <Layout>
            <div className="flex flex-col items-center justify-center">
                <h1 className="text-4xl">I2C</h1>
            </div>
        </Layout>
    );
};

export default I2C;
