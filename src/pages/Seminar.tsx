import { FC } from "react";
import Layout from "../components/Layout";

const Seminar: FC = () => {
    return (
        <Layout>
            <div className="flex flex-col items-center justify-center">
                <h1 className="text-4xl">Seminar</h1>
            </div>
        </Layout>
    );
};

export default Seminar;
